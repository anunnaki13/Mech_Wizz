import { AppShell } from "@/components/layout/AppShell";

export default function SensitivityPage() {
  return (
    <AppShell>
      <section className="page-header">
        <div>
          <h2>Strategy Dashboard & Ranking</h2>
          <p>This capability is planned for a later phase after scenario inputs and calculations exist.</p>
        </div>
      </section>
      <div className="notice">Ranking and sensitivity views are not available in Phase 1.</div>
    </AppShell>
  );
}
