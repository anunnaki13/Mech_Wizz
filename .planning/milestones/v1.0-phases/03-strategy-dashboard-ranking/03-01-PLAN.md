---
phase: 03-strategy-dashboard-ranking
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0004_phase3_scoring.py
  - backend/app/models/unit_scoring_result.py
  - backend/app/models/__init__.py
  - backend/app/schemas/scoring.py
  - backend/app/schemas/__init__.py
  - backend/app/routers/scoring.py
  - backend/app/routers/map.py
  - backend/app/routers/unit_profiles.py
  - backend/app/main.py
  - backend/app/seed.py
  - backend/app/services/scoring.py
  - backend/app/services/map_geojson.py
  - backend/app/services/unit_profile.py
  - backend/tests/test_scoring.py
autonomous: true
requirements:
  - DATA-09
  - UNIT-07
  - SCORE-01
  - SCORE-02
  - SCORE-03
  - SCORE-04
  - SCORE-05
  - SCORE-06
  - SCORE-07
user_setup: []
must_haves:
  truths:
    - "D-01: Scoring consumes persisted ScenarioResult records as numeric source of truth."
    - "D-02: No frontend score or simulation recalculation is introduced."
    - "D-03: APIs keep Phase 1 plants as the unit/site identity."
    - "D-04: Tenayan seed coordinates may be updated from the addendum."
    - "D-05: ScenarioResult missing_inputs and confidence metadata propagate into scoring/profile responses."
    - "D-06: UnitScoringResult records are persisted with plant_id, scenario_id, and scenario_result_id."
    - "D-07: Recalculate creates the latest scoring set and rank positions."
    - "D-08: Opportunity score uses addendum component weights."
    - "D-09: Readiness score uses addendum component weights."
    - "D-10: Confidence score uses addendum data-status mapping."
    - "D-11: Composite score is 45/35/20."
    - "D-12: Heatmap weight includes opportunity, readiness, economic return, and confidence."
    - "D-13: Scoring formulas live in backend services and are tested."
    - "D-14: Ranking rows include all addendum-required fields."
    - "D-15: Required scoring/map/profile API paths are implemented using plant_id."
    - "D-16: GeoJSON comes from backend scoring/scenario data."
    - "D-17: Unit profile combines plant, inputs, scenario result, scoring, and bottleneck metadata."
    - "D-18: Recalculation responds to selected scenario and scheme filters."
  artifacts:
    - path: "backend/app/models/unit_scoring_result.py"
      provides: "Persisted scoring output"
      contains: "class UnitScoringResult"
    - path: "backend/app/services/scoring.py"
      provides: "Opportunity/readiness/confidence/composite score logic"
      contains: "calculate_unit_scores"
    - path: "backend/app/routers/map.py"
      provides: "GeoJSON endpoint"
      contains: "unit-opportunity"
---

<objective>
Build backend scoring, ranking, GeoJSON, and selected unit profile APIs.
</objective>

<context>
@.planning/phases/03-strategy-dashboard-ranking/03-CONTEXT.md
@.planning/phases/03-strategy-dashboard-ranking/03-RESEARCH.md
@.planning/phases/03-strategy-dashboard-ranking/03-PATTERNS.md
@docs/MECH_WIZ_Heatmap_Map_Addendum.md
@.planning/phases/02-scenario-simulation-engine/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UnitScoringResult model and scoring services</name>
  <files>backend/alembic/versions/0004_phase3_scoring.py, backend/app/models/unit_scoring_result.py, backend/app/models/__init__.py, backend/app/schemas/scoring.py, backend/app/schemas/__init__.py, backend/app/services/scoring.py, backend/tests/test_scoring.py</files>
  <action>Create `UnitScoringResult` with score components, composite, heatmap_weight, data_gap_count, rank_position, recommended_scheme, key_bottleneck, scoring_version, and timestamps. Implement normalization, confidence mapping, opportunity score, readiness score, confidence score, composite score, economic return score, heatmap weight, bottleneck derivation, and scoring persistence from latest ScenarioResult.</action>
  <verify>cd backend && pytest -q tests/test_scoring.py -k "score or confidence"</verify>
</task>

<task type="auto">
  <name>Task 2: Add ranking, recalculation, GeoJSON, and profile APIs</name>
  <files>backend/app/routers/scoring.py, backend/app/routers/map.py, backend/app/routers/unit_profiles.py, backend/app/main.py, backend/app/services/map_geojson.py, backend/app/services/unit_profile.py, backend/tests/test_scoring.py</files>
  <action>Add `POST /api/scoring/recalculate`, `GET /api/scoring/unit-ranking`, `GET /api/map/unit-opportunity`, and `GET /api/units/{plant_id}/profile`. Ranking and GeoJSON must filter by scenario/scheme/region/fuel/confidence/opportunity when provided. Profile must include plant, latest result, latest scoring result, confidence labels, and score-derived bottlenecks.</action>
  <verify>cd backend && pytest -q tests/test_scoring.py -k "api or geojson or profile"</verify>
</task>

<task type="auto">
  <name>Task 3: Seed Tenayan coordinates and verify backend scoring slice</name>
  <files>backend/app/seed.py, backend/tests/test_scoring.py, .planning/phases/03-strategy-dashboard-ranking/03-01-SUMMARY.md</files>
  <action>Update Tenayan seed with latitude `0.5123` and longitude `101.5567` when missing/seeded. Ensure scoring tests can seed Tenayan, run scenario simulation, recalculate score, read ranking, read GeoJSON, and read unit profile.</action>
  <verify>cd backend && pytest -q tests/test_scoring.py</verify>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q tests/test_scoring.py` exits 0.
- [ ] `rg -n "UnitScoringResult|calculate_unit_scores|unit-opportunity|unit-ranking" backend` returns matches.
- [ ] `DATABASE_URL=sqlite:////tmp/mechwiz_phase3_scoring.db backend/.venv/bin/alembic upgrade head` exits 0.
</verification>

<output>
Create `.planning/phases/03-strategy-dashboard-ranking/03-01-SUMMARY.md`.
</output>
