---
phase: 02-scenario-simulation-engine
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - backend/alembic/versions/0003_phase2_scenario_results.py
  - backend/app/models/scenario_result.py
  - backend/app/models/__init__.py
  - backend/app/schemas/scenario_result.py
  - backend/app/schemas/__init__.py
  - backend/app/routers/scenarios.py
  - backend/app/services/scenario_simulation.py
  - backend/tests/test_simulation.py
  - frontend/types/scenario-result.ts
  - frontend/lib/api.ts
  - frontend/components/scenarios/ScenarioWorkspace.tsx
  - frontend/components/scenarios/SimulationResultPanel.tsx
  - frontend/app/scenarios/page.tsx
  - README.md
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-05
  - CALC-06
  - CALC-07
  - CALC-08
  - CALC-09
  - CALC-10
  - UNIT-05
  - UNIT-06
user_setup: []
must_haves:
  truths:
    - "D-04: Simulation uses Phase 1 plants as the plant/unit identity source."
    - "D-05: Simulation reads existing emission test fields and plant operating days."
    - "D-06: Phase 2 does not split plants into addendum plant_sites or plant_units."
    - "D-14: Technical outputs still calculate when emission and plant operating data exist."
    - "D-15: Financial outputs may be null when required assumptions are missing."
    - "D-16: Null or benchmark-driven financial outputs are low-confidence and expose missing/assumption context."
    - "D-17: Simulation tests include a complete-assumptions case proving CALC-09 end-to-end."
    - "D-21: Simulation endpoint persists scenario results linked to a business scenario."
    - "D-22: Scenario results store technical and financial outputs needed by Phase 3 plus confidence and calculation version."
    - "D-23: Scenario results store assumption snapshot/metadata sufficient for reproducible reloads."
    - "D-24: Frontend can run a simulation from /scenarios and display stored result fields."
    - "D-25: Users can select a plant, create/read scenarios, edit assumptions, run simulation, and see persisted results."
    - "D-27: Scenario result labels show actual, estimated, benchmark, or user-assumption source context."
    - "No Phase 3 map/ranking/scoring UI is implemented in Phase 2."
  artifacts:
    - path: "backend/app/models/scenario_result.py"
      provides: "ScenarioResult ORM model"
      contains: "captured_co2_ton_per_year"
    - path: "backend/app/services/scenario_simulation.py"
      provides: "Simulation orchestration service"
      contains: "run_scenario_simulation"
    - path: "frontend/components/scenarios/SimulationResultPanel.tsx"
      provides: "Scenario result UI"
      contains: "Captured CO2"
---

<objective>
Wire deterministic calculations into persisted scenario simulations and the frontend workflow.

Purpose: Let users run a reproducible simulation from stored plant, emission, scenario, and financial assumptions, then reload the saved result.
Output: Scenario result model/API, simulation orchestration service, tests, frontend run/display flow, final Phase 2 verification docs.
</objective>

<context>
@.planning/phases/02-scenario-simulation-engine/02-CONTEXT.md
@.planning/phases/02-scenario-simulation-engine/02-RESEARCH.md
@.planning/phases/02-scenario-simulation-engine/02-PATTERNS.md
@.planning/phases/02-scenario-simulation-engine/02-UI-SPEC.md
@docs/blueprint.md
@docs/MECH_WIZ_Heatmap_Map_Addendum.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scenario result persistence and simulation endpoint</name>
  <files>backend/alembic/versions/0003_phase2_scenario_results.py, backend/app/models/scenario_result.py, backend/app/models/__init__.py, backend/app/schemas/scenario_result.py, backend/app/schemas/__init__.py, backend/app/routers/scenarios.py, backend/app/services/scenario_simulation.py, backend/tests/test_simulation.py</files>
  <action>Create `ScenarioResult` model with `scenario_id`, total/captured/vented CO2, methanol, H2 required, electrolyzer MW, gross revenue, LCOM, NPV, IRR, payback, `missing_inputs` or equivalent serialized metadata, `calculation_version`, `confidence_level`, and `created_at`. Add `POST /api/scenarios/{scenario_id}/simulate`, `GET /api/scenarios/{scenario_id}/results`, and `GET /api/scenario-results/{result_id}`. The simulation service must load scenario, plant, emission tests, and financial assumptions; call calculation services; persist one result per run; and return the stored result.</action>
  <verify>cd backend && pytest -q tests/test_simulation.py</verify>
  <acceptance_criteria>
    - `backend/app/models/scenario_result.py` contains `class ScenarioResult`.
    - `backend/app/services/scenario_simulation.py` contains `run_scenario_simulation`.
    - `backend/app/routers/scenarios.py` contains `/simulate`.
    - `backend/tests/test_simulation.py` contains `test_run_simulation_persists_result`.
    - `backend/tests/test_simulation.py` contains `test_run_simulation_marks_missing_financial_inputs`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add scenario result frontend run/display flow</name>
  <files>frontend/types/scenario-result.ts, frontend/lib/api.ts, frontend/components/scenarios/ScenarioWorkspace.tsx, frontend/components/scenarios/SimulationResultPanel.tsx, frontend/app/scenarios/page.tsx</files>
  <action>Add `ScenarioResult` type and API helpers `runScenarioSimulation`, `getScenarioResults`, and `getScenarioResult`. Update `ScenarioWorkspace` to enable `Run Simulation` for selected scenario, refresh latest results, and render `SimulationResultPanel`. The panel must show technical and financial outputs with null-safe labels and confidence/missing-input warnings.</action>
  <verify>cd frontend && npm run build</verify>
  <acceptance_criteria>
    - `frontend/lib/api.ts` contains `runScenarioSimulation`.
    - `frontend/components/scenarios/ScenarioWorkspace.tsx` contains `Run Simulation`.
    - `frontend/components/scenarios/SimulationResultPanel.tsx` contains `Captured CO2`.
    - `frontend/components/scenarios/SimulationResultPanel.tsx` contains `E-Methanol`.
    - `frontend/components/scenarios/SimulationResultPanel.tsx` contains `H2 Required`.
    - `frontend/components/scenarios/SimulationResultPanel.tsx` contains `LCOM`.
    - `frontend/components/scenarios/SimulationResultPanel.tsx` contains `NPV`.
    - `frontend/components/scenarios/SimulationResultPanel.tsx` contains `IRR`.
    - `frontend/components/scenarios/SimulationResultPanel.tsx` contains `Payback`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final Phase 2 verification and documentation</name>
  <files>README.md, .planning/phases/02-scenario-simulation-engine/02-03-SUMMARY.md, .planning/phases/02-scenario-simulation-engine/02-VERIFICATION.md, .planning/phases/02-scenario-simulation-engine/02-REVIEW.md</files>
  <action>Update README with Phase 2 migration/seed/simulation verification notes if needed. Run all backend tests, frontend build, Docker Compose config, and Alembic upgrade against a temporary SQLite DB. Write Phase 2 verification and review artifacts after implementation.</action>
  <verify>docker compose config && cd backend && pytest -q && cd ../frontend && npm run build</verify>
  <acceptance_criteria>
    - `.planning/phases/02-scenario-simulation-engine/02-VERIFICATION.md` exists.
    - `.planning/phases/02-scenario-simulation-engine/02-REVIEW.md` exists.
    - `README.md` contains `alembic upgrade head`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `docker compose config` exits 0.
- [ ] `cd backend && pytest -q` exits 0.
- [ ] `cd frontend && npm run build` exits 0.
- [ ] `DATABASE_URL=sqlite:////tmp/mechwiz_phase2_full.db .venv/bin/alembic upgrade head` exits 0.
- [ ] `rg -n "runScenarioSimulation|ScenarioResult|Captured CO2|calculate_irr" backend frontend` returns matches.
</verification>

<output>
After completion, create `.planning/phases/02-scenario-simulation-engine/02-03-SUMMARY.md`, `.planning/phases/02-scenario-simulation-engine/02-VERIFICATION.md`, and `.planning/phases/02-scenario-simulation-engine/02-REVIEW.md`.
</output>
