---
phase: 02-scenario-simulation-engine
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/services/__init__.py
  - backend/app/services/calculations/__init__.py
  - backend/app/services/calculations/co2.py
  - backend/app/services/calculations/methanol.py
  - backend/app/services/calculations/hydrogen.py
  - backend/app/services/calculations/financial.py
  - backend/app/services/calculations/defaults.py
  - backend/tests/test_calculations.py
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-05
  - CALC-06
  - CALC-07
  - CALC-08
  - CALC-09
user_setup: []
must_haves:
  truths:
    - "D-01: Calculation functions are deterministic backend code and are the numeric authority."
    - "D-02: The frontend may display results but must not recalculate CO2, methanol, H2, revenue, LCOM, NPV, IRR, or payback."
    - "D-03: LLM is not introduced for Phase 2 calculation work."
    - "D-17: Calculation tests include a complete-assumptions case proving CALC-09."
    - "D-18: Methanol, H2, electrolyzer, revenue, LCOM, NPV, IRR, and payback calculations use blueprint defaults."
    - "D-19: Capture rate is an explicit scenario/simulation input, with 0.85 only as labeled benchmark/default."
    - "D-20: Project life for NPV/IRR/payback is centralized and documented when used as a service default."
  artifacts:
    - path: "backend/app/services/calculations/co2.py"
      provides: "CO2 calculation functions"
      contains: "calculate_stack_area_m2"
    - path: "backend/app/services/calculations/financial.py"
      provides: "Financial calculation functions"
      contains: "calculate_irr"
    - path: "backend/tests/test_calculations.py"
      provides: "Formula unit tests"
      contains: "test_calculate_co2_ton_year"
---

<objective>
Build deterministic calculation services for Phase 2.

Purpose: Centralize technical and financial formulas in backend services so later APIs, map ranking, investor dashboards, and LLM summaries consume authoritative stored outputs.
Output: Pure calculation modules and tests for CO2, captured CO2, methanol, H2, electrolyzer, revenue, LCOM, NPV, IRR, and payback.
</objective>

<context>
@.planning/phases/02-scenario-simulation-engine/02-CONTEXT.md
@.planning/phases/02-scenario-simulation-engine/02-RESEARCH.md
@docs/blueprint.md
@docs/MECH_WIZ_Heatmap_Map_Addendum.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CO2 stack calculation service</name>
  <files>backend/app/services/__init__.py, backend/app/services/calculations/__init__.py, backend/app/services/calculations/defaults.py, backend/app/services/calculations/co2.py, backend/tests/test_calculations.py</files>
  <action>Implement formula functions: `calculate_stack_area_m2(diameter_m)`, `calculate_normalized_gas_flow_nm3_s(velocity_m_s, area_m2, temperature_c)`, `calculate_co2_wet_fraction(co2_percent_dry, moisture_percent)`, `calculate_co2_kg_s(normal_flow_nm3_s, co2_wet_fraction, co2_density_kg_nm3=1.964)`, `calculate_co2_ton_day(co2_kg_s)`, `calculate_co2_ton_year(co2_ton_day, operating_days_per_year)`, and aggregate helper for multiple emission tests. Validate missing/invalid inputs return explicit `None` or raise controlled `ValueError` as tests define.</action>
  <verify>cd backend && pytest -q tests/test_calculations.py -k co2</verify>
  <acceptance_criteria>
    - `backend/app/services/calculations/co2.py` contains `calculate_stack_area_m2`.
    - `backend/app/services/calculations/co2.py` contains `calculate_co2_ton_year`.
    - `backend/tests/test_calculations.py` contains `test_calculate_co2_ton_year`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement methanol, H2, and electrolyzer services</name>
  <files>backend/app/services/calculations/methanol.py, backend/app/services/calculations/hydrogen.py, backend/app/services/calculations/defaults.py, backend/tests/test_calculations.py</files>
  <action>Implement captured/vented CO2, methanol theoretical/actual output, H2 required, and electrolyzer MW functions using blueprint defaults: CO2 to methanol factor `0.7273`, process efficiency `0.60`, H2 per methanol `0.1875`, H2 utilization `0.90`, H2 productivity `480 kg/day/MW`.</action>
  <verify>cd backend && pytest -q tests/test_calculations.py -k "methanol or hydrogen"</verify>
  <acceptance_criteria>
    - `backend/app/services/calculations/methanol.py` contains `calculate_methanol_actual_ton_year`.
    - `backend/app/services/calculations/hydrogen.py` contains `calculate_h2_required_ton_year`.
    - `backend/app/services/calculations/hydrogen.py` contains `calculate_electrolyzer_mw`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement financial service</name>
  <files>backend/app/services/calculations/financial.py, backend/app/services/calculations/defaults.py, backend/tests/test_calculations.py</files>
  <action>Implement gross revenue, annualized CAPEX via capital recovery factor, annual OPEX, H2 cost, electricity cost, LCOM, NPV, IRR, and payback. Use local deterministic IRR implementation rather than LLM. Return `None` for IRR/payback when cashflows do not support a valid value. Keep all assumptions explicit in function inputs.</action>
  <verify>cd backend && pytest -q tests/test_calculations.py -k financial</verify>
  <acceptance_criteria>
    - `backend/app/services/calculations/financial.py` contains `calculate_gross_revenue_usd_per_year`.
    - `backend/app/services/calculations/financial.py` contains `calculate_lcom_usd_per_ton`.
    - `backend/app/services/calculations/financial.py` contains `calculate_npv`.
    - `backend/app/services/calculations/financial.py` contains `calculate_irr`.
    - `backend/app/services/calculations/financial.py` contains `calculate_payback_years`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q tests/test_calculations.py` exits 0.
- [ ] `rg -n "calculate_stack_area_m2|calculate_methanol_actual_ton_year|calculate_irr|calculate_payback_years" backend/app/services backend/tests` returns matches.
</verification>

<output>
After completion, create `.planning/phases/02-scenario-simulation-engine/02-02-SUMMARY.md`.
</output>
