---
phase: 08-offtake-mrv-readiness
plan: "01"
type: execute
wave: 1
depends_on: ["07-03"]
files_modified:
  - backend/alembic/versions/0010_phase8_offtake_mrv.py
  - backend/app/models/pre_feed_market.py
  - backend/app/models/__init__.py
  - backend/app/schemas/pre_feed_market.py
  - backend/app/services/prefeed_market.py
  - backend/app/routers/prefeed_market.py
  - backend/app/main.py
  - backend/tests/test_prefeed_market.py
  - .planning/phases/08-offtake-mrv-readiness/08-01-SUMMARY.md
autonomous: true
requirements:
  - OFFT-01
  - OFFT-02
  - OFFT-03
  - OFFT-04
must_haves:
  truths:
    - "D-01: Offtake prospects belong to a Pre-FEED package."
    - "D-02: Price decks belong to a Pre-FEED package and may reference the scenario."
    - "D-04: Phase 8 returns scenario-ready patches/summaries and does not mutate historical scenario tables."
    - "D-05: Supporting documents reuse the existing `documents` table."
    - "D-06: Offtake records capture counterparty, product, target volume, term, pricing, status, document, status/confidence, and notes."
    - "D-07: Product vocabulary starts with e_methanol, carbon_credit, co2_supply, hydrogen, and other."
    - "D-08: Offtake status vocabulary starts with lead, discussion, loi, term_sheet, contracted, signed, and inactive."
    - "D-09: Price decks capture market assumptions and active state."
    - "D-10: Only one price deck should be active per package."
    - "D-11: Revenue summary is deterministic backend code."
    - "D-12: Revenue summary may return a scenario-ready patch without writing scenario history."
    - "D-13: Offtake readiness score is rule-based."
    - "D-14: Methanol volume coverage uses latest stored ScenarioResult when available."
    - "D-15: Offtake gaps flag missing prospects, price deck, market prices, target volume, pricing basis, weak status, documents, and low confidence."
  artifacts:
    - path: "backend/app/models/pre_feed_market.py"
      provides: "Offtake and price deck persistence"
      contains: "PreFeedOfftakeProspect"
    - path: "backend/app/services/prefeed_market.py"
      provides: "Revenue and readiness summary"
      contains: "build_offtake_summary"
---

<objective>
Implement Phase 8 offtake prospects, price decks, deterministic revenue summary, offtake readiness, and tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add offtake and price deck persistence</name>
  <files>backend/alembic/versions/0010_phase8_offtake_mrv.py, backend/app/models/pre_feed_market.py, backend/app/models/__init__.py</files>
  <action>Create package-scoped offtake prospect and price deck tables/models with active price deck support. Cover D-01, D-02, D-05, D-06, D-07, D-08, D-09, and D-10.</action>
  <acceptance_criteria>Fresh Alembic upgrade creates the new tables and indexes.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add schemas, service, router</name>
  <files>backend/app/schemas/pre_feed_market.py, backend/app/services/prefeed_market.py, backend/app/routers/prefeed_market.py, backend/app/main.py</files>
  <action>Add CRUD endpoints and deterministic offtake summary/readiness/gap logic. Cover D-04, D-11, D-12, D-13, D-14, and D-15.</action>
  <acceptance_criteria>Endpoints live under `/api/prefeed` and do not mutate scenario history.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add tests and summary</name>
  <files>backend/tests/test_prefeed_market.py, .planning/phases/08-offtake-mrv-readiness/08-01-SUMMARY.md</files>
  <action>Test offtake CRUD, active deck behavior, revenue summary, readiness, gaps, and no financial assumption mutation.</action>
  <acceptance_criteria>Targeted backend tests pass.</acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q tests/test_prefeed_market.py`
- [ ] Fresh Alembic upgrade exits 0.
- [ ] `rg -n "PreFeedOfftakeProspect|price-decks|offtake-summary|OFFT-04" backend .planning/phases/08-offtake-mrv-readiness`
</verification>
