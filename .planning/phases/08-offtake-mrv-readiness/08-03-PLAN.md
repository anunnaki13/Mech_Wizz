---
phase: 08-offtake-mrv-readiness
plan: "03"
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - frontend/types/prefeed-market.ts
  - frontend/lib/api.ts
  - frontend/components/prefeed/OfftakeMrvWorkspace.tsx
  - frontend/components/prefeed/PreFeedWorkspace.tsx
  - frontend/app/globals.css
  - README.md
  - .planning/phases/08-offtake-mrv-readiness/08-03-SUMMARY.md
  - .planning/phases/08-offtake-mrv-readiness/08-VERIFICATION.md
  - .planning/phases/08-offtake-mrv-readiness/08-REVIEW.md
autonomous: true
requirements:
  - OFFT-01
  - OFFT-02
  - OFFT-03
  - OFFT-04
  - MRV-01
  - MRV-02
  - MRV-03
  - MRV-04
must_haves:
  truths:
    - "D-22: Extend existing `/prefeed` rather than adding a new route."
    - "D-23: Add a compact Offtake & MRV workspace below the existing cost/vendor panels."
    - "D-24: Frontend renders backend outputs and does not recalculate."
    - "D-25: UI uses compact operational panels."
  artifacts:
    - path: "frontend/components/prefeed/OfftakeMrvWorkspace.tsx"
      provides: "Phase 8 Pre-FEED workspace UI"
      contains: "OfftakeMrvWorkspace"
    - path: "frontend/types/prefeed-market.ts"
      provides: "Typed frontend contract for Phase 8 APIs"
      contains: "PreFeedOfftakeProspect"
---

<objective>
Extend `/prefeed` with offtake, price deck, MRV, readiness, revenue, carbon, and gap UI plus final Phase 8 verification docs.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend Phase 8 types and API helpers</name>
  <files>frontend/types/prefeed-market.ts, frontend/lib/api.ts</files>
  <action>Add typed helpers for offtake prospect CRUD, price deck CRUD/activate, summaries, MRV CRUD, and gaps.</action>
  <acceptance_criteria>Helpers preserve existing `apiFetch` behavior and snake_case backend fields.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Offtake/MRV workspace panels</name>
  <files>frontend/components/prefeed/OfftakeMrvWorkspace.tsx, frontend/components/prefeed/PreFeedWorkspace.tsx, frontend/app/globals.css</files>
  <action>Add package-scoped Phase 8 forms, tables, summary cards, and gap panels. Cover D-22, D-23, D-24, and D-25.</action>
  <acceptance_criteria>`/prefeed` remains a usable operational workspace and Phase 8 outputs come from backend APIs.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final Phase 8 docs and verification</name>
  <files>README.md, .planning/phases/08-offtake-mrv-readiness/08-03-SUMMARY.md, .planning/phases/08-offtake-mrv-readiness/08-VERIFICATION.md, .planning/phases/08-offtake-mrv-readiness/08-REVIEW.md</files>
  <action>Document workflow/endpoints and verify every Phase 8 requirement.</action>
  <acceptance_criteria>Frontend build and full backend tests pass; verification maps every Phase 8 requirement.</acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `docker compose config`
- [ ] `cd backend && pytest -q`
- [ ] `cd frontend && npm run build`
- [ ] Fresh Alembic upgrade exits 0.
- [ ] `rg -n "OfftakeMrvWorkspace|PreFeedOfftakeProspect|PreFeedMrvAssumption|OFFT-01|MRV-04" backend frontend .planning/phases/08-offtake-mrv-readiness README.md`
</verification>
