---
phase: 07-cost-vendor-proposal-engine
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/app/models/pre_feed_cost.py
  - backend/app/schemas/pre_feed_cost.py
  - backend/app/services/prefeed_costs.py
  - backend/app/routers/prefeed_costs.py
  - backend/tests/test_prefeed_costs.py
autonomous: true
requirements:
  - COST-03
  - VEND-01
  - VEND-02
  - VEND-03
  - VEND-04
must_haves:
  truths:
    - "D-10: Active cost-basis selection is a separate auditable selection record."
    - "D-11: Prior active selections are retained and marked inactive."
    - "D-12: Vendor proposals belong to a Pre-FEED package and may reference an uploaded document."
    - "D-13: Vendor proposal metadata fields are implemented."
    - "D-14: Proposal scope coverage uses structured booleans."
    - "D-15: Proposal comparison is deterministic."
    - "D-16: Vendor proposal gaps are deterministic and rule-based."
    - "D-17: Phase 7 proposal gap rules cover required missing scopes and metadata."
    - "D-18: MRV scope is only recorded as scope coverage in Phase 7."
  artifacts:
    - path: "backend/app/services/prefeed_costs.py"
      provides: "Vendor comparison, gaps, and active cost-basis selection"
      contains: "build_vendor_comparison"
    - path: "backend/app/routers/prefeed_costs.py"
      provides: "Vendor proposal and active cost basis API"
      contains: "active-cost-basis"
---

<objective>
Implement vendor proposal CRUD, deterministic vendor comparison/gaps, active cost-basis selection, and backend tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add vendor proposal schemas and service behavior</name>
  <files>backend/app/schemas/pre_feed_cost.py, backend/app/services/prefeed_costs.py</files>
  <read_first>backend/app/models/pre_feed_cost.py, backend/app/services/prefeed.py, backend/app/schemas/document.py</read_first>
  <action>Add vendor proposal create/update/read schemas and service helpers for proposal validation, supporting document validation, scope completeness, proposal gaps, and deterministic comparison rows.</action>
  <acceptance_criteria>Comparison rows include totals, missing scopes, scope completeness, gap count, and confidence without frontend calculation.</acceptance_criteria>
  <verify>rg -n "VendorProposal|build_vendor_comparison|generate_vendor_gaps|scope_completeness" backend/app</verify>
</task>

<task type="auto">
  <name>Task 2: Add vendor and active cost-basis endpoints</name>
  <files>backend/app/routers/prefeed_costs.py</files>
  <read_first>backend/app/routers/prefeed.py, backend/app/routers/scenarios.py</read_first>
  <action>Add proposal list/create/update/delete, proposal gaps, package vendor comparison, active cost-basis get/select endpoints, and error handling.</action>
  <acceptance_criteria>Active cost-basis selection creates a new active snapshot and marks prior scenario selections inactive without mutating financial assumptions or scenario results.</acceptance_criteria>
  <verify>rg -n "vendor-proposals|vendor-comparison|active-cost-basis" backend/app/routers/prefeed_costs.py</verify>
</task>

<task type="auto">
  <name>Task 3: Add vendor comparison and selection tests</name>
  <files>backend/tests/test_prefeed_costs.py</files>
  <read_first>backend/tests/test_prefeed_costs.py, backend/tests/test_prefeed.py</read_first>
  <action>Cover proposal CRUD, supporting document validation, gap output, comparison output, active cost-basis selection, prior selection history, and non-mutation of scenario history.</action>
  <acceptance_criteria>Targeted tests prove `VEND-01` through `VEND-04` and active cost-basis history behavior.</acceptance_criteria>
  <verify>cd backend && pytest -q tests/test_prefeed_costs.py</verify>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q tests/test_prefeed_costs.py` exits 0.
- [ ] `rg -n "VendorProposal|vendor-comparison|active-cost-basis|missing_scopes" backend` returns matches.
</verification>

<output>
Create `.planning/phases/07-cost-vendor-proposal-engine/07-02-SUMMARY.md`.
</output>
