---
phase: 07-cost-vendor-proposal-engine
plan: "01"
type: execute
wave: 1
depends_on: ["06-03"]
files_modified:
  - backend/alembic/versions/0009_phase7_cost_vendor.py
  - backend/app/models/pre_feed_cost.py
  - backend/app/models/__init__.py
  - backend/app/schemas/pre_feed_cost.py
  - backend/app/services/prefeed_costs.py
  - backend/app/routers/prefeed_costs.py
  - backend/app/main.py
  - backend/tests/test_prefeed_costs.py
autonomous: true
requirements:
  - COST-01
  - COST-02
  - COST-03
must_haves:
  truths:
    - "D-01: Cost line items belong to a pre_feed_package_id."
    - "D-02: Use one detailed cost item model/table with cost_type."
    - "D-03: CAPEX items capture component, amount, currency, source, contingency, escalation, data_status, confidence, notes."
    - "D-04: OPEX items capture category, amount, currency, unit basis, recurrence, source, data_status, confidence, notes."
    - "D-05: CAPEX component vocabulary is implemented."
    - "D-06: OPEX category vocabulary is implemented."
    - "D-07: Aggregation is deterministic backend code."
    - "D-08: Aggregation returns totals by currency without silent FX conversion."
    - "D-09: USD aggregation exposes scenario-ready patch fields without mutating FinancialAssumption."
    - "D-10: Active cost basis is not implemented as a FinancialAssumption overwrite."
    - "D-21: Frontend must not calculate totals."
  artifacts:
    - path: "backend/app/models/pre_feed_cost.py"
      provides: "Pre-FEED cost item model"
      contains: "class PreFeedCostItem"
    - path: "backend/app/services/prefeed_costs.py"
      provides: "Deterministic cost aggregation"
      contains: "build_cost_summary"
---

<objective>
Implement Phase 7 cost item persistence, cost CRUD APIs, deterministic package cost aggregation, and targeted backend tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 7 cost/vendor migration and models</name>
  <files>backend/alembic/versions/0009_phase7_cost_vendor.py, backend/app/models/pre_feed_cost.py, backend/app/models/__init__.py</files>
  <read_first>backend/alembic/versions/0008_phase6_prefeed_packages.py, backend/app/models/pre_feed_package.py, backend/app/models/financial_assumption.py</read_first>
  <action>Create SQLite-compatible Phase 7 tables with cost items, vendor proposals, and cost-basis selection foundations. Register `PreFeedCostItem` model exports.</action>
  <acceptance_criteria>Migration upgrades from a fresh SQLite database and cost item records retain package/source/confidence fields.</acceptance_criteria>
  <verify>rm -f /tmp/mechwiz_phase7_plan01.db && cd backend && DATABASE_URL=sqlite:////tmp/mechwiz_phase7_plan01.db .venv/bin/alembic upgrade head</verify>
</task>

<task type="auto">
  <name>Task 2: Add cost schemas, service, and routes</name>
  <files>backend/app/schemas/pre_feed_cost.py, backend/app/services/prefeed_costs.py, backend/app/routers/prefeed_costs.py, backend/app/main.py</files>
  <read_first>backend/app/schemas/pre_feed.py, backend/app/services/prefeed.py, backend/app/routers/prefeed.py, backend/app/main.py</read_first>
  <action>Add cost item create/update/read schemas, package cost item CRUD/list endpoints, and cost summary endpoint with backend-only aggregation.</action>
  <acceptance_criteria>Cost summary returns CAPEX totals by currency, annual OPEX totals by currency, and USD scenario-ready patch fields without writing to `FinancialAssumption`.</acceptance_criteria>
  <verify>rg -n "PreFeedCostItem|build_cost_summary|cost-items|cost-summary" backend/app</verify>
</task>

<task type="auto">
  <name>Task 3: Add cost aggregation tests</name>
  <files>backend/tests/test_prefeed_costs.py</files>
  <read_first>backend/tests/test_prefeed.py, backend/tests/test_data_quality_settings.py</read_first>
  <action>Cover CAPEX/OPEX cost item creation, listing, update/delete, package aggregation, recurrence annualization, USD scenario-ready patch, and non-mutation of `FinancialAssumption`.</action>
  <acceptance_criteria>Targeted tests prove `COST-01`, `COST-02`, and `COST-03` backend behavior.</acceptance_criteria>
  <verify>cd backend && pytest -q tests/test_prefeed_costs.py</verify>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q tests/test_prefeed_costs.py` exits 0.
- [ ] Fresh Alembic upgrade exits 0.
- [ ] `rg -n "PreFeedCostItem|pre_feed_cost_items|cost-summary|capex_capture_usd" backend` returns matches.
</verification>

<output>
Create `.planning/phases/07-cost-vendor-proposal-engine/07-01-SUMMARY.md`.
</output>
