---
phase: 07-cost-vendor-proposal-engine
plan: "03"
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - frontend/types/prefeed-cost.ts
  - frontend/lib/api.ts
  - frontend/components/prefeed/CostVendorWorkspace.tsx
  - frontend/components/prefeed/PreFeedWorkspace.tsx
  - frontend/app/globals.css
  - README.md
  - .planning/phases/07-cost-vendor-proposal-engine/07-03-SUMMARY.md
  - .planning/phases/07-cost-vendor-proposal-engine/07-VERIFICATION.md
  - .planning/phases/07-cost-vendor-proposal-engine/07-REVIEW.md
autonomous: true
requirements:
  - COST-01
  - COST-02
  - COST-03
  - VEND-01
  - VEND-02
  - VEND-03
  - VEND-04
must_haves:
  truths:
    - "D-19: Extend existing /prefeed workspace rather than creating a separate route."
    - "D-20: Add compact cost/vendor panels for line items, proposal scope, comparison, and active cost-basis selection."
    - "D-21: UI displays backend aggregation and gap outputs and does not recalculate totals/ranking/gaps."
    - "D-03: CAPEX fields are exposed in the UI."
    - "D-04: OPEX fields are exposed in the UI."
    - "D-14: Vendor proposal scope booleans are exposed in the UI."
    - "D-15: Proposal comparison output is visible."
    - "D-17: Proposal gaps are visible."
  artifacts:
    - path: "frontend/components/prefeed/CostVendorWorkspace.tsx"
      provides: "Cost/vendor proposal workspace section"
      contains: "CostVendorWorkspace"
    - path: "frontend/types/prefeed-cost.ts"
      provides: "Typed frontend contract for Phase 7 APIs"
      contains: "PreFeedCostItem"
---

<objective>
Extend `/prefeed` with cost item, vendor proposal, comparison, and active cost-basis UI plus final Phase 7 verification artifacts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend Phase 7 types and API helpers</name>
  <files>frontend/types/prefeed-cost.ts, frontend/lib/api.ts</files>
  <read_first>frontend/types/prefeed.ts, frontend/lib/api.ts</read_first>
  <action>Add typed helpers for cost item CRUD, cost summary, vendor proposal CRUD, vendor comparison/gaps, and active cost-basis get/select.</action>
  <acceptance_criteria>Types match backend snake_case fields and helpers preserve `apiFetch` behavior.</acceptance_criteria>
  <verify>rg -n "PreFeedCostItem|VendorProposal|activeCostBasis|vendorComparison" frontend/types frontend/lib/api.ts</verify>
</task>

<task type="auto">
  <name>Task 2: Add cost/vendor workspace panels</name>
  <files>frontend/components/prefeed/CostVendorWorkspace.tsx, frontend/components/prefeed/PreFeedWorkspace.tsx, frontend/app/globals.css</files>
  <read_first>frontend/components/prefeed/PreFeedWorkspace.tsx, frontend/app/globals.css, frontend/components/settings/SettingsWorkspace.tsx</read_first>
  <action>Add a child workspace showing cost forms/tables, aggregate summary, proposal form/table, comparison rows, warnings, and active cost-basis selection for the selected package/scenario.</action>
  <acceptance_criteria>`/prefeed` remains an operational tool surface, Phase 6 controls still render, and Phase 7 totals/comparison/gaps come from backend API responses.</acceptance_criteria>
  <verify>cd frontend && npm run build</verify>
</task>

<task type="auto">
  <name>Task 3: Final Phase 7 verification and docs</name>
  <files>README.md, .planning/phases/07-cost-vendor-proposal-engine/07-03-SUMMARY.md, .planning/phases/07-cost-vendor-proposal-engine/07-VERIFICATION.md, .planning/phases/07-cost-vendor-proposal-engine/07-REVIEW.md</files>
  <read_first>README.md, .planning/phases/07-cost-vendor-proposal-engine/07-CONTEXT.md, .planning/phases/07-cost-vendor-proposal-engine/07-UI-SPEC.md, .planning/REQUIREMENTS.md</read_first>
  <action>Document the cost/vendor workflow and API endpoints, verify requirements `COST-01` through `VEND-04`, and record residual review risks.</action>
  <acceptance_criteria>Verification maps every Phase 7 requirement to code evidence and commands.</acceptance_criteria>
  <verify>docker compose config && cd backend && pytest -q && cd ../frontend && npm run build</verify>
</task>

</tasks>

<verification>
- [ ] `docker compose config` exits 0.
- [ ] `cd backend && pytest -q` exits 0.
- [ ] `cd frontend && npm run build` exits 0.
- [ ] Fresh Alembic upgrade exits 0.
- [ ] `rg -n "PreFeedCostItem|VendorProposal|CostVendorWorkspace|active-cost-basis|COST-01|VEND-04" backend frontend .planning/phases/07-cost-vendor-proposal-engine README.md` returns matches.
</verification>

<output>
Create `.planning/phases/07-cost-vendor-proposal-engine/07-03-SUMMARY.md`, `.planning/phases/07-cost-vendor-proposal-engine/07-VERIFICATION.md`, and `.planning/phases/07-cost-vendor-proposal-engine/07-REVIEW.md`.
</output>
