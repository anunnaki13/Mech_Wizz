---
phase: 05-llm-document-intelligence
plan: "01"
type: execute
wave: 1
depends_on: ["04-01", "04-03"]
files_modified:
  - backend/alembic/versions/0007_phase5_llm_documents.py
  - backend/app/config.py
  - backend/app/models/document.py
  - backend/app/models/llm_insight.py
  - backend/app/models/__init__.py
  - backend/app/schemas/llm.py
  - backend/app/routers/llm.py
  - backend/app/main.py
  - backend/app/services/llm.py
  - backend/tests/test_llm.py
autonomous: true
requirements:
  - DATA-07
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
  - LLM-05
  - LLM-06
  - LLM-07
  - SENS-04
must_haves:
  truths:
    - "D-01: OpenRouter is the Phase 5 provider."
    - "D-02: Direct HTTP OpenRouter integration is used for MVP."
    - "D-03: API key stays in backend environment."
    - "D-05: Missing API key fails clearly without fake responses."
    - "D-06: LLM output is narrative only."
    - "D-07: Prompts forbid invented numbers."
    - "D-08: Prompts separate actual data, assumptions, confidence, and data gaps."
    - "D-10: Exact prompt and response are stored."
    - "D-11: Supported insight types are implemented."
  artifacts:
    - path: "backend/app/models/llm_insight.py"
      provides: "Persisted LLM insight records"
      contains: "class LlmInsight"
    - path: "backend/app/services/llm.py"
      provides: "OpenRouter service and prompt builders"
      contains: "Do not invent numbers"
---

<objective>
Implement backend OpenRouter insight generation, prompt guardrails, and LLM insight persistence.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add LLM insight schema and migration</name>
  <files>backend/alembic/versions/0007_phase5_llm_documents.py, backend/app/models/llm_insight.py, backend/app/models/__init__.py, backend/app/schemas/llm.py</files>
  <action>Create `LlmInsight` with scenario/document references, insight type, model name, prompt, response, status, error, usage metadata, and timestamps.</action>
  <verify>cd backend && pytest -q tests/test_llm.py</verify>
</task>

<task type="auto">
  <name>Task 2: Add OpenRouter service and prompt builders</name>
  <files>backend/app/config.py, backend/app/services/llm.py</files>
  <action>Add env settings, OpenRouter HTTP client wrapper, prompt builders, missing-key behavior, and generation persistence.</action>
  <verify>rg -n "OPENROUTER|Do not invent numbers|executive_summary|investor_memo" backend/app</verify>
</task>

<task type="auto">
  <name>Task 3: Add LLM API endpoints and tests</name>
  <files>backend/app/routers/llm.py, backend/app/main.py, backend/tests/test_llm.py</files>
  <action>Add generation endpoints and insight listing. Tests must mock OpenRouter success and verify missing-key failure.</action>
  <verify>cd backend && pytest -q tests/test_llm.py</verify>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q tests/test_llm.py` exits 0.
- [ ] `rg -n "LlmInsight|OpenRouter|Do not invent numbers|document_qa" backend` returns matches.
</verification>

<output>
Create `.planning/phases/05-llm-document-intelligence/05-01-SUMMARY.md`.
</output>
