---
phase: 01-mvp-spine-unit-data
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/models/emission_test.py
  - backend/app/models/__init__.py
  - backend/app/schemas/emission_test.py
  - backend/app/schemas/__init__.py
  - backend/app/routers/emission_tests.py
  - backend/app/routers/plants.py
  - backend/app/main.py
  - backend/app/seed.py
  - backend/tests/test_emission_tests.py
  - frontend/types/emission-test.ts
  - frontend/types/plant.ts
  - frontend/lib/api.ts
  - frontend/components/units/EmissionTestsSection.tsx
  - frontend/app/units/[id]/page.tsx
autonomous: true
requirements:
  - DATA-02
  - DATA-08
  - UNIT-02
user_setup: []
must_haves:
  truths:
    - "D-05: Phase 1 schema includes emission_tests alongside plants, site_readiness, and hydrogen_strategies."
    - "D-06: Emission test records use UUID primary keys, timestamps, data_status, and confidence_level."
    - "D-07: Emission test data_status and confidence_level are persisted fields, not frontend-only labels."
    - "D-08: Emission test schema is migration-ready and does not rely only on ad hoc schema creation."
    - "D-09: Tenayan seed includes the two chimney emission test entries from docs/blueprint.md."
    - "D-10: Rerunning the seed does not create duplicate Tenayan chimney rows."
    - "D-11: Missing Tenayan emission values remain null or explicitly unknown / low confidence."
    - "D-15: Unit profile UI includes readable emission form/detail sections."
    - "D-16: Emission test endpoints use REST/JSON matching the blueprint direction."
    - "D-17: Emission test frontend form fields align with backend schemas and validation."
    - "D-18: Emission test data is stored without CO2, methanol, H2, financial, ranking, or sensitivity calculations."
    - "User can view emission test records for a plant."
    - "User can add an emission test record for a plant through the API."
    - "Seeded Tenayan data includes the two chimney emission test records from the blueprint."
    - "Emission test records include data_status and confidence_level."
  artifacts:
    - path: "backend/app/models/emission_test.py"
      provides: "EmissionTest ORM model"
      contains: "co2_percent_dry"
    - path: "backend/app/routers/emission_tests.py"
      provides: "Emission test API endpoints"
      contains: "emission-tests"
    - path: "frontend/components/units/EmissionTestsSection.tsx"
      provides: "Emission test UI section"
      contains: "Emission Tests"
  key_links:
    - from: "frontend/components/units/EmissionTestsSection.tsx"
      to: "/api/plants/{id}/emission-tests"
      via: "frontend/lib/api.ts"
      pattern: "getEmissionTests"
    - from: "backend/app/routers/emission_tests.py"
      to: "backend/app/models/emission_test.py"
      via: "SQLAlchemy relationship to Plant"
      pattern: "EmissionTest"
---

<objective>
Add the emission test vertical slice for Phase 1.

Purpose: Extend the walking skeleton from plant records to stack/emission inputs, including backend storage/API, seeded Tenayan chimney data, and the unit profile UI section.
Output: Emission test model, schemas, endpoints, seed data, tests, frontend type/API helper, and emission tests UI section.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-spine-unit-data/01-CONTEXT.md
@.planning/phases/01-mvp-spine-unit-data/01-RESEARCH.md
@.planning/phases/01-mvp-spine-unit-data/01-UI-SPEC.md
@.planning/phases/01-mvp-spine-unit-data/01-PATTERNS.md
@.planning/phases/01-mvp-spine-unit-data/01-01-SUMMARY.md
@docs/blueprint.md
</context>

<decision_coverage>
- D-05: Implement Phase 1 schema around `plants`, `emission_tests`, `site_readiness`, and `hydrogen_strategies` first.
- D-06: Emission test records use UUID primary keys, timestamps, `data_status`, and `confidence_level`.
- D-07: Store `data_status` and `confidence_level` as constrained application values and visible persisted fields.
- D-08: Prefer Alembic-style migrations over ad hoc schema creation as the database evolves.
- D-09: Tenayan seed includes the two chimney emission test entries from `docs/blueprint.md`.
- D-10: Rerunning the seed does not create duplicate Tenayan chimney rows.
- D-11: Missing Tenayan values remain null or explicitly marked `unknown` / low confidence.
- D-15: Unit profile UI prioritizes readable forms and detail sections for emission data.
- D-16: Emission test APIs use REST/JSON matching the blueprint endpoint direction.
- D-17: Backend validation and frontend forms stay aligned for emission test fields.
- D-18: Phase 1 keeps calculations out; emission data is stored but not converted into CO2/methanol outputs.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add emission test model, schema, and endpoints</name>
  <files>backend/app/models/emission_test.py, backend/app/models/__init__.py, backend/app/schemas/emission_test.py, backend/app/schemas/__init__.py, backend/app/routers/emission_tests.py, backend/app/routers/plants.py, backend/app/main.py, backend/tests/test_emission_tests.py</files>
  <read_first>backend/app/models/plant.py, backend/app/schemas/common.py, backend/app/routers/plants.py, backend/app/main.py, docs/blueprint.md</read_first>
  <action>Create SQLAlchemy `EmissionTest` model with UUID `id`, `plant_id` foreign key to plants with cascade delete, `stack_id`, `test_date`, `lab_name`, `stack_diameter_m`, `gas_velocity_m_s`, `flue_gas_temperature_c`, `co2_percent_dry`, `o2_percent`, `moisture_percent`, `so2_mg_nm3`, `nox_mg_nm3`, `particulate_mg_nm3`, `hg_mg_nm3`, `compliance_status`, `data_status`, `confidence_level`, and `created_at`. Create Pydantic `EmissionTestCreate`, `EmissionTestUpdate`, and `EmissionTestRead`. Add endpoints `GET /api/plants/{plant_id}/emission-tests`, `POST /api/plants/{plant_id}/emission-tests`, `PUT /api/emission-tests/{emission_test_id}`, and `DELETE /api/emission-tests/{emission_test_id}`. Tests must create a plant, add an emission test, list it, update it, and delete it.</action>
  <verify>cd backend && pytest -q backend/tests/test_emission_tests.py</verify>
  <acceptance_criteria>
    - `backend/app/models/emission_test.py` contains `class EmissionTest`.
    - `backend/app/models/emission_test.py` contains `co2_percent_dry`.
    - `backend/app/routers/emission_tests.py` contains `/emission-tests`.
    - `backend/app/routers/emission_tests.py` contains `@router.post`.
    - `backend/tests/test_emission_tests.py` contains `test_create_list_update_delete_emission_test`.
  </acceptance_criteria>
  <done>Emission test CRUD works through backend tests.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Tenayan seed with blueprint chimney records</name>
  <files>backend/app/seed.py, backend/tests/test_emission_tests.py</files>
  <read_first>backend/app/seed.py, backend/app/models/plant.py, backend/app/models/emission_test.py, docs/blueprint.md</read_first>
  <action>Update `python -m app.seed` so it creates or updates exactly two Tenayan emission tests: `stack_id="Chimney #1"` with `stack_diameter_m=3.0`, `gas_velocity_m_s=14.4`, `flue_gas_temperature_c=124`, `co2_percent_dry=8.48`, `o2_percent=9.54`, `moisture_percent=5.84`, `so2_mg_nm3=233`, `nox_mg_nm3=263`, `particulate_mg_nm3=55.4`; and `stack_id="Chimney #2"` with `stack_diameter_m=3.0`, `gas_velocity_m_s=13.5`, `flue_gas_temperature_c=109`, `co2_percent_dry=3.54`, `o2_percent=15.0`, `moisture_percent=6.48`, `so2_mg_nm3=268`, `nox_mg_nm3=202`, `particulate_mg_nm3=52.3`. Use `data_status="actual"` and `confidence_level="medium"` unless the blueprint provides a more specific value. Ensure rerunning the seed does not create duplicate chimney rows for the same Tenayan plant.</action>
  <verify>cd backend && pytest -q backend/tests/test_emission_tests.py -k seed</verify>
  <acceptance_criteria>
    - `backend/app/seed.py` contains `Chimney #1`.
    - `backend/app/seed.py` contains `Chimney #2`.
    - `backend/app/seed.py` contains `8.48`.
    - `backend/app/seed.py` contains `3.54`.
    - `backend/tests/test_emission_tests.py` contains `test_seed_tenayan_emission_tests_idempotent`.
  </acceptance_criteria>
  <done>Tenayan seed includes exactly two idempotent chimney records.</done>
</task>

<task type="auto">
  <name>Task 3: Add emission tests to unit profile UI</name>
  <files>frontend/types/emission-test.ts, frontend/types/plant.ts, frontend/lib/api.ts, frontend/components/units/EmissionTestsSection.tsx, frontend/app/units/[id]/page.tsx</files>
  <read_first>frontend/lib/api.ts, frontend/types/plant.ts, frontend/app/dashboard/page.tsx, .planning/phases/01-mvp-spine-unit-data/01-UI-SPEC.md</read_first>
  <action>Create `EmissionTest` TypeScript type matching `EmissionTestRead`. Add API helpers `getPlant(id: string)`, `getEmissionTests(plantId: string)`, and `createEmissionTest(plantId: string, payload: Partial<EmissionTest>)`. Create `/units/[id]` page that loads the plant and emission tests. Create `EmissionTestsSection` with a dense table showing `stack_id`, `co2_percent_dry`, `gas_velocity_m_s`, `flue_gas_temperature_c`, `data_status`, and `confidence_level`, plus an `Add Emission Test` button wired to a form/dialog or inline form. Keep styling aligned with UI-SPEC dark enterprise shell.</action>
  <verify>cd frontend && npm run build</verify>
  <acceptance_criteria>
    - `frontend/types/emission-test.ts` contains `export type EmissionTest`.
    - `frontend/lib/api.ts` contains `getEmissionTests`.
    - `frontend/lib/api.ts` contains `createEmissionTest`.
    - `frontend/components/units/EmissionTestsSection.tsx` contains `Add Emission Test`.
    - `frontend/components/units/EmissionTestsSection.tsx` contains `confidence_level`.
    - `frontend/app/units/[id]/page.tsx` contains `EmissionTestsSection`.
  </acceptance_criteria>
  <done>Unit profile page shows and can add emission test data through the API.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && pytest -q backend/tests/test_emission_tests.py` exits 0.
- [ ] `cd frontend && npm run build` exits 0.
- [ ] `rg -n "Chimney #1|Chimney #2|EmissionTestsSection|getEmissionTests" backend frontend` returns matches.
</verification>

<success_criteria>
- Emission test records persist with data status and confidence.
- Tenayan seed includes two chimney records from the blueprint.
- Unit profile UI can display and add emission tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-spine-unit-data/01-02-SUMMARY.md`
</output>
