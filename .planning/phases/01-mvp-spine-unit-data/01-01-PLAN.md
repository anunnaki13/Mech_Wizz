---
phase: 01-mvp-spine-unit-data
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - README.md
  - backend/Dockerfile
  - backend/requirements.txt
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/database.py
  - backend/app/models/__init__.py
  - backend/app/models/plant.py
  - backend/app/schemas/__init__.py
  - backend/app/schemas/common.py
  - backend/app/schemas/plant.py
  - backend/app/routers/__init__.py
  - backend/app/routers/health.py
  - backend/app/routers/plants.py
  - backend/app/seed.py
  - backend/tests/test_health_plants.py
  - frontend/Dockerfile
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/next.config.ts
  - frontend/app/layout.tsx
  - frontend/app/page.tsx
  - frontend/app/dashboard/page.tsx
  - frontend/components/layout/AppShell.tsx
  - frontend/lib/api.ts
  - frontend/types/plant.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - DATA-01
  - DATA-08
  - UNIT-01
user_setup: []
must_haves:
  truths:
    - "D-01: Monorepo layout includes backend, frontend, docs, planning docs, docker-compose.yml, README.md, and .env.example."
    - "D-02: Phase 1 uses FastAPI Python backend, Next.js React TypeScript frontend, and PostgreSQL database."
    - "D-03: Docker Compose starts PostgreSQL, backend API, and frontend web services together."
    - "D-04: Backend API is mounted under /api and includes a lightweight health endpoint."
    - "D-06: Plant records use UUID primary keys, timestamps, data_status, and confidence_level."
    - "D-09: PLTU Tenayan is seeded from docs/blueprint.md as the initial demonstration unit."
    - "D-10: Tenayan seed behavior is repeatable and does not create uncontrolled duplicate plant records."
    - "D-12: Phase 1 UI is a functional enterprise dashboard shell, not a marketing page."
    - "D-13: Dashboard shell uses dark navy, cyan/teal accents, compact KPI cards, clean typography, and professional navigation."
    - "D-14: /dashboard shows enough seeded Tenayan/unit summary data to prove backend-to-frontend integration."
    - "D-16: Plant endpoints use REST/JSON matching the blueprint direction."
    - "D-17: Backend returns structured validation errors and frontend plant display aligns with backend schemas."
    - "Developer can run PostgreSQL, backend, and frontend through Docker Compose."
    - "GET /api/health returns a JSON object with status ok."
    - "GET /api/plants returns the seeded PLTU Tenayan plant after the seed command runs."
    - "POST /api/plants creates a plant record with data_status and confidence_level."
    - "Opening /dashboard loads plant data from the backend API instead of hardcoded frontend data."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Full-stack local run configuration"
      contains: "postgres"
    - path: "backend/app/main.py"
      provides: "FastAPI app with /api routers"
      contains: "FastAPI"
    - path: "backend/app/models/plant.py"
      provides: "Plant ORM model"
      contains: "data_status"
    - path: "backend/app/seed.py"
      provides: "Idempotent Tenayan seed"
      contains: "PLTU Tenayan"
    - path: "frontend/app/dashboard/page.tsx"
      provides: "Dashboard route loading backend data"
      contains: "getPlants"
  key_links:
    - from: "frontend/app/dashboard/page.tsx"
      to: "/api/plants"
      via: "frontend/lib/api.ts fetch wrapper"
      pattern: "getPlants"
    - from: "backend/app/routers/plants.py"
      to: "backend/app/models/plant.py"
      via: "SQLAlchemy session query/create"
      pattern: "Plant"
---

<objective>
Build the walking skeleton for MECH WIZ AI Digital Twin.

Purpose: Establish the monorepo, Docker Compose services, FastAPI API shell, first PostgreSQL-backed plant record slice, Tenayan seed, and dashboard route that proves frontend-to-backend-to-database connectivity.
Output: Runnable backend/frontend/database stack with `/api/health`, `/api/plants`, idempotent Tenayan seed, and `/dashboard` showing real plant data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-spine-unit-data/01-CONTEXT.md
@.planning/phases/01-mvp-spine-unit-data/01-RESEARCH.md
@.planning/phases/01-mvp-spine-unit-data/01-UI-SPEC.md
@.planning/phases/01-mvp-spine-unit-data/01-PATTERNS.md
@docs/blueprint.md
</context>

<decision_coverage>
- D-01: Use monorepo layout with `backend/`, `frontend/`, `docs/`, `.planning/`, `docker-compose.yml`, `README.md`, and `.env.example`.
- D-02: Use FastAPI + Python backend, Next.js + React + TypeScript frontend, and PostgreSQL database.
- D-03: Docker Compose starts PostgreSQL, backend API, and frontend web services together.
- D-04: Backend API is mounted under `/api` and includes a health/version endpoint.
- D-06: Plant records use UUID primary keys, timestamps, `data_status`, and `confidence_level`.
- D-09: Seed PLTU Tenayan from `docs/blueprint.md` as the first demonstration unit.
- D-10: Seed behavior is repeatable and avoids uncontrolled duplicate Tenayan records.
- D-12: Phase 1 UI is a functional enterprise dashboard shell, not a marketing page.
- D-13: UI uses dark navy, cyan/teal accents, compact KPI cards, clean typography, and professional dashboard navigation.
- D-14: `/dashboard` proves backend-to-frontend integration with seeded Tenayan/unit summary data.
- D-16: Plant APIs use REST/JSON matching the blueprint endpoint direction.
- D-17: Backend returns structured validation errors and frontend forms align with backend schemas.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Create full-stack project scaffold and Compose runtime</name>
  <files>docker-compose.yml, .env.example, README.md, backend/Dockerfile, backend/requirements.txt, backend/app/__init__.py, backend/app/main.py, backend/app/config.py, backend/app/database.py, frontend/Dockerfile, frontend/package.json, frontend/tsconfig.json, frontend/next.config.ts, frontend/app/layout.tsx, frontend/app/page.tsx</files>
  <read_first>.planning/phases/01-mvp-spine-unit-data/01-CONTEXT.md, .planning/phases/01-mvp-spine-unit-data/01-RESEARCH.md, docs/blueprint.md</read_first>
  <action>Create `docker-compose.yml` with services named `db`, `backend`, and `frontend`. Use PostgreSQL database `mechwiz`, user `mechwiz`, password `mechwiz_dev`, expose backend on port `8000`, frontend on port `3000`, and mount source directories for local development. Create `.env.example` containing `DATABASE_URL=postgresql+psycopg://mechwiz:mechwiz_dev@db:5432/mechwiz`, `BACKEND_URL=http://localhost:8000`, `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api`, `APP_ENV=development`, `JWT_SECRET=change_this_secret`, and `UPLOAD_DIR=/app/uploads`. Create backend FastAPI scaffold with settings in `config.py`, database engine/session in `database.py`, and API app in `main.py`. Create frontend Next scaffold with `/` redirecting to `/dashboard` in `frontend/app/page.tsx`. Document `docker compose up --build`, backend test command, and frontend build command in `README.md`.</action>
  <verify>test -f docker-compose.yml && test -f backend/app/main.py && test -f frontend/app/page.tsx && rg -n "NEXT_PUBLIC_API_BASE_URL|DATABASE_URL|docker compose up --build" .env.example README.md</verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains `db:`, `backend:`, and `frontend:`.
    - `.env.example` contains `DATABASE_URL=postgresql+psycopg://mechwiz:mechwiz_dev@db:5432/mechwiz`.
    - `backend/app/main.py` contains `FastAPI`.
    - `frontend/app/page.tsx` contains `redirect("/dashboard")` or `redirect('/dashboard')`.
    - `README.md` contains `docker compose up --build`.
  </acceptance_criteria>
  <done>Full-stack scaffold files exist and the documented local run path is present.</done>
</task>

<task type="auto">
  <name>Task 2: Implement plant model, schemas, API, and Tenayan seed</name>
  <files>backend/app/models/__init__.py, backend/app/models/plant.py, backend/app/schemas/__init__.py, backend/app/schemas/common.py, backend/app/schemas/plant.py, backend/app/routers/__init__.py, backend/app/routers/health.py, backend/app/routers/plants.py, backend/app/main.py, backend/app/seed.py, backend/tests/test_health_plants.py</files>
  <read_first>backend/app/main.py, backend/app/config.py, backend/app/database.py, docs/blueprint.md, .planning/phases/01-mvp-spine-unit-data/01-CONTEXT.md</read_first>
  <action>Define allowed values exactly as `DATA_STATUS_VALUES = ("actual", "estimated", "benchmark", "user_assumption", "unknown", "partner_supplied")` and `CONFIDENCE_LEVEL_VALUES = ("high", "medium", "low", "unknown")` in `backend/app/schemas/common.py`. Create SQLAlchemy `Plant` model with UUID `id`, `plant_name`, `unit_name`, `province`, `city`, `latitude`, `longitude`, `capacity_mw`, `fuel_type`, `status`, `capacity_factor`, `operating_days_per_year`, `owner`, `data_status`, `confidence_level`, `created_at`, and `updated_at`. Add Pydantic `PlantCreate`, `PlantUpdate`, and `PlantRead` schemas. Add `GET /api/health`, `GET /api/plants`, `POST /api/plants`, `GET /api/plants/{plant_id}`, `PUT /api/plants/{plant_id}`, and `DELETE /api/plants/{plant_id}`. Add `python -m app.seed` idempotent seed that creates or updates PLTU Tenayan with `unit_name="Unit 1-2"`, `province="Riau"`, `city="Pekanbaru"`, `capacity_mw=220`, `fuel_type="coal"`, `status="active"`, `capacity_factor=0.90`, `operating_days_per_year=330`, `owner="PLN NP"`, `data_status="actual"`, and `confidence_level="medium"`.</action>
  <verify>cd backend && pytest -q</verify>
  <acceptance_criteria>
    - `backend/app/models/plant.py` contains `class Plant`.
    - `backend/app/schemas/common.py` contains `DATA_STATUS_VALUES`.
    - `backend/app/schemas/common.py` contains `partner_supplied`.
    - `backend/app/routers/plants.py` contains `@router.get("/")`.
    - `backend/app/routers/plants.py` contains `@router.post("/")`.
    - `backend/app/seed.py` contains `PLTU Tenayan`.
    - `backend/tests/test_health_plants.py` contains `test_seed_tenayan`.
  </acceptance_criteria>
  <done>Backend exposes health and plant CRUD endpoints, and Tenayan seed is idempotent.</done>
</task>

<task type="auto">
  <name>Task 3: Wire dashboard shell to backend plant data</name>
  <files>frontend/app/dashboard/page.tsx, frontend/components/layout/AppShell.tsx, frontend/lib/api.ts, frontend/types/plant.ts, frontend/app/layout.tsx</files>
  <read_first>frontend/app/layout.tsx, frontend/app/page.tsx, .planning/phases/01-mvp-spine-unit-data/01-UI-SPEC.md, .planning/phases/01-mvp-spine-unit-data/01-CONTEXT.md</read_first>
  <action>Create `frontend/lib/api.ts` with `API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8000/api"` and an `apiFetch<T>(path: string, init?: RequestInit): Promise<T>` helper that throws `Error("API request failed")` for non-2xx responses. Create `getPlants()` calling `/plants`. Create `frontend/types/plant.ts` with a `Plant` type containing every field returned by `PlantRead`. Create `AppShell` with nav labels `Dashboard`, `Units`, `Scenarios`, `Investor`, `Sensitivity`, `Documents`, `Settings`. Build `/dashboard` as a dark premium dashboard shell that calls `getPlants()`, displays Tenayan capacity/fuel/owner/data status/confidence if present, and shows a clear placeholder string `Simulation insights available after Phase 2` for unfinished calculations.</action>
  <verify>cd frontend && npm run build</verify>
  <acceptance_criteria>
    - `frontend/lib/api.ts` contains `NEXT_PUBLIC_API_BASE_URL`.
    - `frontend/lib/api.ts` contains `export async function getPlants`.
    - `frontend/components/layout/AppShell.tsx` contains `MECH WIZ AI Digital Twin`.
    - `frontend/components/layout/AppShell.tsx` contains `Dashboard`.
    - `frontend/app/dashboard/page.tsx` contains `getPlants`.
    - `frontend/app/dashboard/page.tsx` contains `Simulation insights available after Phase 2`.
    - `frontend/app/dashboard/page.tsx` does not contain hardcoded `IRR`, `NPV`, or `LCOM` numeric values.
  </acceptance_criteria>
  <done>Dashboard route renders real backend-loaded plant data and avoids fake later-phase metrics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose config` exits 0.
- [ ] `cd backend && pytest -q` exits 0.
- [ ] `cd frontend && npm run build` exits 0.
- [ ] `rg -n "PLTU Tenayan|DATA_STATUS_VALUES|NEXT_PUBLIC_API_BASE_URL" backend frontend .env.example` returns matches.
</verification>

<success_criteria>
- Full-stack scaffold exists.
- Backend health and plant CRUD APIs exist under `/api`.
- Tenayan seed is idempotent.
- `/dashboard` loads plant data through the API.
- No Phase 2+ economics are faked in Phase 1 dashboard code.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-spine-unit-data/01-01-SUMMARY.md`
</output>
