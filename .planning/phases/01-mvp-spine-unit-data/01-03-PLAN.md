---
phase: 01-mvp-spine-unit-data
plan: "03"
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - backend/app/models/site_readiness.py
  - backend/app/models/hydrogen_strategy.py
  - backend/app/models/__init__.py
  - backend/app/schemas/site_readiness.py
  - backend/app/schemas/hydrogen_strategy.py
  - backend/app/schemas/__init__.py
  - backend/app/routers/site_readiness.py
  - backend/app/routers/hydrogen_strategy.py
  - backend/app/main.py
  - backend/tests/test_site_readiness_hydrogen.py
  - frontend/types/site-readiness.ts
  - frontend/types/hydrogen-strategy.ts
  - frontend/lib/api.ts
  - frontend/components/units/UnitList.tsx
  - frontend/components/units/SiteReadinessSection.tsx
  - frontend/components/units/HydrogenStrategySection.tsx
  - frontend/app/units/page.tsx
  - frontend/app/units/[id]/page.tsx
  - frontend/app/scenarios/page.tsx
  - frontend/app/investor/page.tsx
  - frontend/app/sensitivity/page.tsx
  - frontend/app/documents/page.tsx
  - frontend/app/settings/page.tsx
autonomous: true
requirements:
  - DATA-03
  - DATA-04
  - UNIT-03
  - UNIT-04
  - PLAT-04
user_setup: []
must_haves:
  truths:
    - "D-05: Phase 1 schema is completed with site_readiness and hydrogen_strategies."
    - "D-06: Site readiness and hydrogen strategy records use UUID primary keys, timestamps, data_status, and confidence_level."
    - "D-07: Readiness and hydrogen data_status and confidence_level are persisted fields, not frontend-only labels."
    - "D-08: Readiness and hydrogen schema changes are migration-ready and avoid ad hoc schema creation."
    - "D-11: Missing readiness or hydrogen values remain null or explicitly unknown / low confidence."
    - "D-12: Unit management UI remains a functional enterprise dashboard shell, not a marketing page."
    - "D-13: Unit management UI uses dark navy, cyan/teal accents, compact panels, clean typography, and professional navigation."
    - "D-14: /dashboard and /units prove backend-to-frontend integration with real unit data."
    - "D-15: Unit profile UI includes readable plant, emission, site readiness, and hydrogen strategy sections."
    - "D-16: Site readiness and hydrogen endpoints use REST/JSON matching the blueprint direction."
    - "D-17: Readiness and hydrogen frontend forms align with backend schemas and validation."
    - "D-18: Route placeholders do not fake CO2, methanol, H2, financial, ranking, or sensitivity outputs."
    - "User can view and update site readiness data for a plant."
    - "User can view and update hydrogen strategy data for a plant."
    - "User can navigate to dashboard, units, scenarios, investor, sensitivity, documents, and settings routes."
    - "Unit profile shows plant identity, emissions, site readiness, and hydrogen strategy sections."
    - "Phase 1 placeholders do not claim future scenario, investor, sensitivity, document, or settings capabilities are complete."
  artifacts:
    - path: "backend/app/models/site_readiness.py"
      provides: "Site readiness ORM model"
      contains: "available_land_ha"
    - path: "backend/app/models/hydrogen_strategy.py"
      provides: "Hydrogen strategy ORM model"
      contains: "h2_strategy"
    - path: "frontend/components/units/SiteReadinessSection.tsx"
      provides: "Site readiness UI section"
      contains: "Site Readiness"
    - path: "frontend/components/units/HydrogenStrategySection.tsx"
      provides: "Hydrogen strategy UI section"
      contains: "Hydrogen Strategy"
  key_links:
    - from: "frontend/components/units/SiteReadinessSection.tsx"
      to: "/api/plants/{id}/site-readiness"
      via: "frontend/lib/api.ts"
      pattern: "getSiteReadiness"
    - from: "frontend/components/units/HydrogenStrategySection.tsx"
      to: "/api/plants/{id}/hydrogen-strategy"
      via: "frontend/lib/api.ts"
      pattern: "getHydrogenStrategy"
---

<objective>
Complete Phase 1 unit input management by adding site readiness, hydrogen strategy, unit list/profile polish, and safe placeholder routes.

Purpose: Finish the user-visible Phase 1 scope so users can manage the unit input data needed before scenario simulation begins in Phase 2.
Output: Site readiness and hydrogen strategy models/APIs/tests, frontend sections/forms, unit list route, complete unit profile composition, and placeholder planned routes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-spine-unit-data/01-CONTEXT.md
@.planning/phases/01-mvp-spine-unit-data/01-RESEARCH.md
@.planning/phases/01-mvp-spine-unit-data/01-UI-SPEC.md
@.planning/phases/01-mvp-spine-unit-data/01-PATTERNS.md
@.planning/phases/01-mvp-spine-unit-data/01-01-SUMMARY.md
@.planning/phases/01-mvp-spine-unit-data/01-02-SUMMARY.md
@docs/blueprint.md
</context>

<decision_coverage>
- D-05: Complete Phase 1 input schema with site readiness and hydrogen strategy alongside plants and emission tests.
- D-06: Site readiness and hydrogen strategy records use UUID primary keys, timestamps, `data_status`, and `confidence_level`.
- D-07: Store `data_status` and `confidence_level` as constrained application values and visible persisted fields.
- D-08: Prefer Alembic-style migrations over ad hoc schema creation as the database evolves.
- D-11: Missing readiness or hydrogen values remain null or explicitly marked `unknown` / low confidence.
- D-12: Unit management UI remains a functional enterprise dashboard shell, not a marketing page.
- D-13: UI uses dark navy, cyan/teal accents, compact panels, clean typography, and professional navigation.
- D-14: `/dashboard` and `/units` continue to prove backend-to-frontend integration with real unit data.
- D-15: Unit profile UI prioritizes readable forms and detail sections for plant, emission, site readiness, and hydrogen strategy data.
- D-16: Site readiness and hydrogen strategy APIs use REST/JSON matching the blueprint endpoint direction.
- D-17: Backend validation and frontend forms stay aligned for readiness and hydrogen fields.
- D-18: Phase 1 placeholders do not calculate or fake CO2, methanol, H2, financial, ranking, or sensitivity outputs.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add site readiness and hydrogen strategy backend slices</name>
  <files>backend/app/models/site_readiness.py, backend/app/models/hydrogen_strategy.py, backend/app/models/__init__.py, backend/app/schemas/site_readiness.py, backend/app/schemas/hydrogen_strategy.py, backend/app/schemas/__init__.py, backend/app/routers/site_readiness.py, backend/app/routers/hydrogen_strategy.py, backend/app/main.py, backend/tests/test_site_readiness_hydrogen.py</files>
  <read_first>backend/app/models/plant.py, backend/app/schemas/common.py, backend/app/routers/emission_tests.py, docs/blueprint.md</read_first>
  <action>Create SQLAlchemy `SiteReadiness` model with UUID `id`, `plant_id`, `available_land_ha`, `land_status`, `distance_to_stack_km`, `has_port_or_jetty`, `distance_to_port_km`, `port_capacity_dwt`, `road_access`, `water_availability`, `power_availability`, `utility_readiness`, `permit_risk`, `social_risk`, `data_status`, `confidence_level`, and `created_at`. Create SQLAlchemy `HydrogenStrategy` model with UUID `id`, `plant_id`, `existing_h2_available`, `h2_strategy`, `h2_cost_case`, `h2_cost_usd_per_kg`, `h2_readiness_score`, `data_status`, `confidence_level`, and `created_at`. Create Pydantic create/update/read schemas. Add endpoints `GET /api/plants/{plant_id}/site-readiness`, `POST /api/plants/{plant_id}/site-readiness`, `PUT /api/site-readiness/{id}`, `GET /api/plants/{plant_id}/hydrogen-strategy`, `POST /api/plants/{plant_id}/hydrogen-strategy`, and `PUT /api/hydrogen-strategy/{id}`. Tests must cover create, get, update for both resources.</action>
  <verify>cd backend && pytest -q backend/tests/test_site_readiness_hydrogen.py</verify>
  <acceptance_criteria>
    - `backend/app/models/site_readiness.py` contains `class SiteReadiness`.
    - `backend/app/models/site_readiness.py` contains `available_land_ha`.
    - `backend/app/models/hydrogen_strategy.py` contains `class HydrogenStrategy`.
    - `backend/app/models/hydrogen_strategy.py` contains `h2_strategy`.
    - `backend/app/routers/site_readiness.py` contains `site-readiness`.
    - `backend/app/routers/hydrogen_strategy.py` contains `hydrogen-strategy`.
    - `backend/tests/test_site_readiness_hydrogen.py` contains `test_site_readiness_create_get_update`.
    - `backend/tests/test_site_readiness_hydrogen.py` contains `test_hydrogen_strategy_create_get_update`.
  </acceptance_criteria>
  <done>Backend stores and updates site readiness and hydrogen strategy records.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit list, readiness, and hydrogen UI sections</name>
  <files>frontend/types/site-readiness.ts, frontend/types/hydrogen-strategy.ts, frontend/lib/api.ts, frontend/components/units/UnitList.tsx, frontend/components/units/SiteReadinessSection.tsx, frontend/components/units/HydrogenStrategySection.tsx, frontend/app/units/page.tsx, frontend/app/units/[id]/page.tsx</files>
  <read_first>frontend/lib/api.ts, frontend/types/plant.ts, frontend/components/units/EmissionTestsSection.tsx, frontend/app/units/[id]/page.tsx, .planning/phases/01-mvp-spine-unit-data/01-UI-SPEC.md</read_first>
  <action>Create `SiteReadiness` and `HydrogenStrategy` TypeScript types matching backend read schemas. Add API helpers `getSiteReadiness`, `saveSiteReadiness`, `getHydrogenStrategy`, and `saveHydrogenStrategy`. Create `/units` page with `UnitList` showing plant name, unit name, province/city, capacity, fuel type, status, data status, and confidence. Update `/units/[id]` to show plant summary, `EmissionTestsSection`, `SiteReadinessSection`, and `HydrogenStrategySection`. Each new section must expose visible `data_status` and `confidence_level` chips and a form or edit action for the Phase 1 fields.</action>
  <verify>cd frontend && npm run build</verify>
  <acceptance_criteria>
    - `frontend/app/units/page.tsx` contains `UnitList`.
    - `frontend/components/units/UnitList.tsx` contains `confidence_level`.
    - `frontend/lib/api.ts` contains `getSiteReadiness`.
    - `frontend/lib/api.ts` contains `saveSiteReadiness`.
    - `frontend/lib/api.ts` contains `getHydrogenStrategy`.
    - `frontend/lib/api.ts` contains `saveHydrogenStrategy`.
    - `frontend/components/units/SiteReadinessSection.tsx` contains `Site Readiness`.
    - `frontend/components/units/HydrogenStrategySection.tsx` contains `Hydrogen Strategy`.
    - `frontend/app/units/[id]/page.tsx` contains `HydrogenStrategySection`.
  </acceptance_criteria>
  <done>Users can navigate unit records and manage readiness/hydrogen strategy inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Add safe route placeholders and final Phase 1 checks</name>
  <files>frontend/app/scenarios/page.tsx, frontend/app/investor/page.tsx, frontend/app/sensitivity/page.tsx, frontend/app/documents/page.tsx, frontend/app/settings/page.tsx, frontend/components/layout/AppShell.tsx, README.md</files>
  <read_first>frontend/components/layout/AppShell.tsx, frontend/app/dashboard/page.tsx, .planning/phases/01-mvp-spine-unit-data/01-UI-SPEC.md, .planning/ROADMAP.md</read_first>
  <action>Create placeholder pages for `/scenarios`, `/investor`, `/sensitivity`, `/documents`, and `/settings`. Each placeholder must use restrained app-shell styling and include copy that says the capability is planned for a later phase, with exact phase names: `Scenario Simulation Engine`, `Investor Case & Data Quality`, `Strategy Dashboard & Ranking`, or `LLM & Document Intelligence` as applicable. Update `AppShell` navigation links to all planned routes. Update `README.md` with a Phase 1 verification section listing `docker compose config`, `cd backend && pytest -q`, and `cd frontend && npm run build`.</action>
  <verify>cd frontend && npm run build && cd ../backend && pytest -q</verify>
  <acceptance_criteria>
    - `frontend/app/scenarios/page.tsx` contains `Scenario Simulation Engine`.
    - `frontend/app/investor/page.tsx` contains `Investor Case & Data Quality`.
    - `frontend/app/sensitivity/page.tsx` contains `Strategy Dashboard & Ranking`.
    - `frontend/app/documents/page.tsx` contains `LLM & Document Intelligence`.
    - `frontend/app/settings/page.tsx` contains `Investor Case & Data Quality`.
    - `frontend/components/layout/AppShell.tsx` contains `/documents`.
    - `README.md` contains `cd backend && pytest -q`.
    - `README.md` contains `cd frontend && npm run build`.
  </acceptance_criteria>
  <done>All planned routes exist safely and Phase 1 verification commands are documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && pytest -q` exits 0.
- [ ] `cd frontend && npm run build` exits 0.
- [ ] `docker compose config` exits 0.
- [ ] `rg -n "SiteReadiness|HydrogenStrategy|Scenario Simulation Engine|LLM & Document Intelligence" backend frontend README.md` returns matches.
</verification>

<success_criteria>
- Site readiness and hydrogen strategy persist through backend APIs.
- Unit list/profile UI exposes plant, emission, readiness, and hydrogen strategy sections.
- All planned app routes exist and clearly avoid false claims about future capabilities.
- README documents Phase 1 verification commands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-spine-unit-data/01-03-SUMMARY.md`
</output>
