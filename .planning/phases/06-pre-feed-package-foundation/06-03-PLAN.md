---
phase: 06-pre-feed-package-foundation
plan: "03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - frontend/types/prefeed.ts
  - frontend/lib/api.ts
  - frontend/app/prefeed/page.tsx
  - frontend/components/prefeed/PreFeedWorkspace.tsx
  - frontend/components/layout/AppShell.tsx
  - frontend/app/globals.css
  - README.md
  - .planning/phases/06-pre-feed-package-foundation/06-03-SUMMARY.md
  - .planning/phases/06-pre-feed-package-foundation/06-VERIFICATION.md
  - .planning/phases/06-pre-feed-package-foundation/06-REVIEW.md
autonomous: true
requirements:
  - PFD-01
  - PFD-02
  - PFD-03
  - PFD-04
  - PFD-05
must_haves:
  truths:
    - "D-01: UI package selection and creation must be anchored to a plant and may reference a scenario."
    - "D-03: The metadata editor exposes package name, status, owner, source organization, received date, version label, data_status, confidence_level, notes, and timestamps where relevant."
    - "D-04: UI links existing uploaded documents instead of adding a second upload path."
    - "D-05: UI supports document role/category assignment for package links."
    - "D-10: UI displays backend-provided deterministic gaps and warnings; it does not generate LLM gap text."
    - "D-11: Gap display uses existing data quality vocabulary."
    - "D-12: Phase 6 warning scope is package metadata and document readiness."
    - "D-13: Add a dedicated /prefeed workspace route."
    - "D-14: The first /prefeed screen is an operational workspace with selectors, metadata editor, linked documents table, and package warnings."
    - "D-15: UI remains consistent with AppShell, compact panels, table/list views, status pills, and null-safe labels."
  artifacts:
    - path: "frontend/components/prefeed/PreFeedWorkspace.tsx"
      provides: "Pre-FEED package workspace"
      contains: "PreFeedWorkspace"
    - path: "frontend/app/prefeed/page.tsx"
      provides: "Dedicated /prefeed route"
      contains: "dynamic = \"force-dynamic\""
---

<objective>
Implement the `/prefeed` frontend workspace, typed API helpers, navigation entry, styling, README note, and final Phase 6 verification/review artifacts.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend Pre-FEED types and API helpers</name>
  <files>frontend/types/prefeed.ts, frontend/lib/api.ts</files>
  <read_first>frontend/types/document.ts, frontend/types/settings.ts, frontend/lib/api.ts</read_first>
  <action>Add TypeScript types and API helpers for package list/create/read/update/archive, document link/list/unlink, and package gaps.</action>
  <acceptance_criteria>Helpers preserve existing `apiFetch` behavior and expose backend snake_case fields without frontend-only recalculation.</acceptance_criteria>
  <verify>rg -n "PreFeedPackage|listPreFeedPackages|archivePreFeedPackage|package gaps" frontend/types frontend/lib/api.ts</verify>
</task>

<task type="auto">
  <name>Task 2: Add /prefeed workspace UI</name>
  <files>frontend/app/prefeed/page.tsx, frontend/components/prefeed/PreFeedWorkspace.tsx, frontend/components/layout/AppShell.tsx, frontend/app/globals.css</files>
  <read_first>frontend/app/documents/page.tsx, frontend/components/documents/DocumentWorkspace.tsx, frontend/components/settings/SettingsWorkspace.tsx, frontend/components/layout/AppShell.tsx, frontend/app/globals.css</read_first>
  <action>Create an operational workspace with plant/scenario/package selectors, package metadata editor, linked document management, backend gap/warning panel, save/new/archive controls, navigation entry, and responsive CSS.</action>
  <acceptance_criteria>`/prefeed` renders as a usable tool surface, follows existing dark dashboard design patterns, and never presents a marketing/landing page.</acceptance_criteria>
  <verify>cd frontend && npm run build</verify>
</task>

<task type="auto">
  <name>Task 3: Final Phase 6 verification and docs</name>
  <files>README.md, .planning/phases/06-pre-feed-package-foundation/06-03-SUMMARY.md, .planning/phases/06-pre-feed-package-foundation/06-VERIFICATION.md, .planning/phases/06-pre-feed-package-foundation/06-REVIEW.md</files>
  <read_first>README.md, .planning/phases/06-pre-feed-package-foundation/06-CONTEXT.md, .planning/phases/06-pre-feed-package-foundation/06-UI-SPEC.md, .planning/REQUIREMENTS.md</read_first>
  <action>Document the new Pre-FEED workspace/API in README, capture implementation summary, run requirement-backed verification, and record any review risks or residual test gaps.</action>
  <acceptance_criteria>Verification explicitly maps `PFD-01` through `PFD-05` to code evidence and all required commands are recorded with pass/fail status.</acceptance_criteria>
  <verify>docker compose config && cd backend && pytest -q && cd ../frontend && npm run build</verify>
</task>

</tasks>

<verification>
- [ ] `docker compose config` exits 0.
- [ ] `cd backend && pytest -q` exits 0.
- [ ] `cd frontend && npm run build` exits 0.
- [ ] Fresh Alembic upgrade exits 0.
- [ ] `rg -n "PreFeedWorkspace|/api/prefeed|PreFeedPackage|pre_feed_packages|PFD-01|PFD-05" backend frontend .planning/phases/06-pre-feed-package-foundation README.md` returns matches.
</verification>

<output>
Create `.planning/phases/06-pre-feed-package-foundation/06-03-SUMMARY.md`, `.planning/phases/06-pre-feed-package-foundation/06-VERIFICATION.md`, and `.planning/phases/06-pre-feed-package-foundation/06-REVIEW.md`.
</output>
