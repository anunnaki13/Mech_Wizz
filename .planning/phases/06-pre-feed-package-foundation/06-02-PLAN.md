---
phase: 06-pre-feed-package-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/app/services/prefeed.py
  - backend/app/routers/prefeed.py
  - backend/app/main.py
  - backend/tests/test_prefeed.py
autonomous: true
requirements:
  - PFD-01
  - PFD-02
  - PFD-03
  - PFD-04
  - PFD-05
must_haves:
  truths:
    - "D-04: Existing Document records are reused; Phase 6 adds an association table rather than a new upload system."
    - "D-05: Package document links include relationship-level document roles."
    - "D-06: Document metadata and extracted text remain owned by the document layer."
    - "D-07: Package APIs must not overwrite ScenarioResult, UnitScoringResult, SensitivityResult, or LLM insight history."
    - "D-08: Package metadata is persisted as its own source-of-truth records."
    - "D-09: Archive behavior is soft status update, not destructive deletion."
    - "D-10: Package-level gap detection is deterministic and rule-based, not LLM-generated."
    - "D-11: Gap output reuses missing data name, source module, impact, priority, recommendation, status, and confidence language."
    - "D-12: Phase 6 gaps cover metadata and document readiness only."
  artifacts:
    - path: "backend/app/routers/prefeed.py"
      provides: "Pre-FEED package REST API"
      contains: "/api/prefeed"
    - path: "backend/app/services/prefeed.py"
      provides: "Deterministic package gap/confidence service"
      contains: "required_document_roles"
---

<objective>
Implement Pre-FEED package REST APIs, document-link endpoints, deterministic package gap service, router registration, and backend tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic Pre-FEED service</name>
  <files>backend/app/services/prefeed.py</files>
  <read_first>backend/app/services/data_quality.py, backend/app/services/settings.py, backend/app/models/pre_feed_package.py, backend/app/schemas/pre_feed.py</read_first>
  <action>Add service helpers for package listing/querying, validation of plant/scenario/document references, package document link rules, soft archive behavior, and deterministic metadata/document-readiness gaps.</action>
  <acceptance_criteria>Gap generation is pure backend logic, never calls LLM code, and returns missing owner/source/received date/version/required document role/low-confidence warnings.</acceptance_criteria>
  <verify>rg -n "generate_package_gaps|required_document_roles|archived" backend/app/services/prefeed.py</verify>
</task>

<task type="auto">
  <name>Task 2: Add Pre-FEED router and register it</name>
  <files>backend/app/routers/prefeed.py, backend/app/main.py</files>
  <read_first>backend/app/routers/documents.py, backend/app/routers/settings.py, backend/app/routers/scenarios.py, backend/app/main.py</read_first>
  <action>Add `/api/prefeed/packages` endpoints for list/create/read/update/archive, package document link/list/unlink, and package gaps. Register router in FastAPI main app.</action>
  <acceptance_criteria>Endpoints return explicit 404/400 errors for missing references or invalid link actions, and package reads include linked document metadata.</acceptance_criteria>
  <verify>rg -n "prefeed|/packages|archive|documents|gaps" backend/app/routers/prefeed.py backend/app/main.py</verify>
</task>

<task type="auto">
  <name>Task 3: Add backend API tests</name>
  <files>backend/tests/test_prefeed.py</files>
  <read_first>backend/tests/test_documents.py, backend/tests/test_data_quality_settings.py, backend/tests/test_llm.py</read_first>
  <action>Cover create/list/read/update/archive package flows, plant/scenario validation, document link/unlink, duplicate document role/link handling, include_archived filtering, and deterministic package gaps.</action>
  <acceptance_criteria>Targeted tests prove APIs satisfy `PFD-01` through `PFD-05` without mutating simulation history tables.</acceptance_criteria>
  <verify>cd backend && pytest -q tests/test_prefeed.py</verify>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q tests/test_prefeed.py` exits 0.
- [ ] `cd backend && pytest -q` exits 0.
- [ ] `rg -n "PreFeedPackage|generate_package_gaps|/api/prefeed|vendor_proposal|epc_estimate" backend` returns matches.
</verification>

<output>
Create `.planning/phases/06-pre-feed-package-foundation/06-02-SUMMARY.md`.
</output>
