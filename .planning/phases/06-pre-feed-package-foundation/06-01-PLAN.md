---
phase: 06-pre-feed-package-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0008_phase6_prefeed_packages.py
  - backend/app/models/pre_feed_package.py
  - backend/app/models/__init__.py
  - backend/app/schemas/pre_feed.py
autonomous: true
requirements:
  - PFD-01
  - PFD-02
  - PFD-03
  - PFD-04
must_haves:
  truths:
    - "D-01: A Pre-FEED package belongs to a plant_id and may optionally reference a scenario_id."
    - "D-02: Phase 6 keeps using the existing Plant identity and does not introduce plant_sites or plant_units."
    - "D-03: Package records include stable UUID, package name, status, owner, source organization, received date, version label, data_status, confidence_level, notes, and timestamps."
    - "D-04: Existing Document records are reused; Phase 6 adds an association table rather than a new upload system."
    - "D-05: Package document links include relationship-level document roles."
    - "D-06: Document metadata and extracted text remain owned by the document layer."
    - "D-07: Package persistence must not overwrite ScenarioResult, UnitScoringResult, SensitivityResult, or LLM insight history."
    - "D-08: Pre-FEED package metadata is its own source of truth."
    - "D-09: Package archival is a soft status, not destructive deletion."
  artifacts:
    - path: "backend/app/models/pre_feed_package.py"
      provides: "Pre-FEED package and package-document ORM models"
      contains: "class PreFeedPackage"
    - path: "backend/alembic/versions/0008_phase6_prefeed_packages.py"
      provides: "SQLite-compatible migration for package tables and indexes"
      contains: "pre_feed_packages"
---

<objective>
Implement the Pre-FEED package data foundation: database migration, SQLAlchemy models, model registration, and Pydantic schemas.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Pre-FEED package migration</name>
  <files>backend/alembic/versions/0008_phase6_prefeed_packages.py</files>
  <read_first>backend/alembic/versions/0007_phase5_llm_documents.py, backend/alembic/versions/0006_phase4_quality_settings.py, backend/app/models/plant.py, backend/app/models/business_scenario.py, backend/app/models/document.py</read_first>
  <action>Create `pre_feed_packages` and `pre_feed_package_documents` with UUID string primary keys, plant/document/scenario foreign keys, timestamps, role/status metadata, and useful indexes for plant/scenario/package lookup.</action>
  <acceptance_criteria>Fresh Alembic upgrade creates both tables and does not touch scenario result, scoring result, sensitivity result, or LLM insight tables.</acceptance_criteria>
  <verify>rm -f /tmp/mechwiz_phase6_plan01.db && cd backend && DATABASE_URL=sqlite:////tmp/mechwiz_phase6_plan01.db .venv/bin/alembic upgrade head</verify>
</task>

<task type="auto">
  <name>Task 2: Add ORM models and relationships</name>
  <files>backend/app/models/pre_feed_package.py, backend/app/models/__init__.py</files>
  <read_first>backend/app/models/document.py, backend/app/models/business_scenario.py, backend/app/models/data_gap.py, backend/app/models/__init__.py</read_first>
  <action>Add `PreFeedPackage` and `PreFeedPackageDocument` models with package metadata, optional scenario reference, package-document role metadata, soft archive status support, and model exports.</action>
  <acceptance_criteria>Models import cleanly through `backend.app.models` and represent document links without duplicating document upload/extraction fields.</acceptance_criteria>
  <verify>cd backend && python - <<'PY'\nfrom app.models import PreFeedPackage, PreFeedPackageDocument\nprint(PreFeedPackage.__tablename__, PreFeedPackageDocument.__tablename__)\nPY</verify>
</task>

<task type="auto">
  <name>Task 3: Add Pydantic schemas</name>
  <files>backend/app/schemas/pre_feed.py</files>
  <read_first>backend/app/schemas/document.py, backend/app/schemas/common.py, backend/app/schemas/application_setting.py</read_first>
  <action>Create create/update/read schemas for packages and package document links. Reuse `data_status` and `confidence_level` vocabulary from existing schemas and include gap read shapes for Plan 06-02.</action>
  <acceptance_criteria>Schemas expose snake_case API fields for every Phase 6 metadata field and support null-safe optional scenario/document metadata in read responses.</acceptance_criteria>
  <verify>cd backend && python -m compileall app/schemas/pre_feed.py</verify>
</task>

</tasks>

<verification>
- [ ] Fresh migration command exits 0.
- [ ] `rg -n "PreFeedPackage|pre_feed_packages|pre_feed_package_documents|document_role" backend/app backend/alembic` returns matches.
- [ ] No Phase 6 model or migration mutates historical scenario/scoring/sensitivity/LLM tables.
</verification>

<output>
Create `.planning/phases/06-pre-feed-package-foundation/06-01-SUMMARY.md`.
</output>
