---
phase: 09-risk-pre-feed-decision-dashboard
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - backend/app/schemas/pre_feed_decision.py
  - backend/app/services/prefeed_decision.py
  - backend/app/routers/prefeed_decision.py
  - backend/tests/test_prefeed_decision.py
  - frontend/types/prefeed-decision.ts
  - frontend/lib/api.ts
  - frontend/components/prefeed/DecisionDashboardWorkspace.tsx
  - frontend/components/prefeed/PreFeedWorkspace.tsx
  - frontend/app/globals.css
  - .planning/phases/09-risk-pre-feed-decision-dashboard/09-02-SUMMARY.md
autonomous: true
requirements:
  - PFDASH-01
  - PFDASH-02
  - PFDASH-03
  - PFDASH-04
  - RISK-01
  - RISK-02
  - RISK-03
  - RISK-04
must_haves:
  truths:
    - "D-11: Blockers are generated deterministically from risks, gates, gaps, and missing active package assumptions."
    - "D-12: Next actions are rule-based and ordered by urgency."
    - "D-13: Add a backend dashboard aggregate endpoint for the selected package."
    - "D-14: The aggregate composes package, cost, vendor, offtake, MRV, risk, gate, blocker, and next-action data."
    - "D-15: The dashboard aggregate reads but does not mutate Phase 1-8 result or assumption tables."
    - "D-16: Missing upstream data is surfaced as warnings and blockers."
    - "D-21: Extend existing `/prefeed` rather than adding a new route."
    - "D-22: Add dashboard near the selected package summary."
    - "D-23: Add compact risk register and decision gate panels."
    - "D-24: Frontend renders backend outputs and does not calculate decision logic."
  artifacts:
    - path: "backend/app/services/prefeed_decision.py"
      provides: "Dashboard aggregate"
      contains: "build_prefeed_decision_dashboard"
    - path: "frontend/components/prefeed/DecisionDashboardWorkspace.tsx"
      provides: "Phase 9 dashboard, risk, and gate UI"
      contains: "DecisionDashboardWorkspace"
---

<objective>
Implement the Pre-FEED decision dashboard aggregate and `/prefeed` dashboard UI with risk/gate management panels.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add backend dashboard aggregate</name>
  <files>backend/app/schemas/pre_feed_decision.py, backend/app/services/prefeed_decision.py, backend/app/routers/prefeed_decision.py, backend/tests/test_prefeed_decision.py</files>
  <action>Add `GET /api/prefeed/packages/{package_id}/decision-dashboard` that composes Phase 6-8 data plus Phase 9 risk/gate summaries. Cover D-11 through D-16.</action>
  <acceptance_criteria>Dashboard returns warnings/blockers for missing upstream data and never mutates history.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add frontend types and API helpers</name>
  <files>frontend/types/prefeed-decision.ts, frontend/lib/api.ts</files>
  <action>Add typed contracts and helpers for risks, gates, summaries, blockers, next actions, and decision dashboard.</action>
  <acceptance_criteria>Helpers preserve existing `apiFetch` behavior and snake_case backend fields.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add dashboard workspace</name>
  <files>frontend/components/prefeed/DecisionDashboardWorkspace.tsx, frontend/components/prefeed/PreFeedWorkspace.tsx, frontend/app/globals.css, .planning/phases/09-risk-pre-feed-decision-dashboard/09-02-SUMMARY.md</files>
  <action>Add decision overview, economics snapshot, blockers, next actions, risk register, and gate panels. Cover D-21 through D-24.</action>
  <acceptance_criteria>`/prefeed` remains operational and dashboard outputs are backend-authored.</acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q tests/test_prefeed_decision.py`
- [ ] `cd frontend && npm run build`
- [ ] `rg -n "DecisionDashboardWorkspace|decision-dashboard|PreFeedRisk|PFDASH-04" backend frontend .planning/phases/09-risk-pre-feed-decision-dashboard`
</verification>
