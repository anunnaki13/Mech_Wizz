---
phase: 04-investor-case-data-quality
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/investor.py
  - backend/app/routers/investor.py
  - backend/app/services/investor_case.py
  - backend/app/main.py
  - backend/tests/test_investor.py
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - QUAL-02
  - QUAL-05
user_setup: []
must_haves:
  truths:
    - "D-01: Investor dashboard consumes backend aggregates, no frontend economics calculation."
    - "D-02: Investor API uses latest persisted scenario result, scoring result, sensitivity results, and assumptions."
    - "D-03: Unavailable financial outputs remain null."
    - "D-04: CAPEX structure is derived from stored assumptions and responsibility fields."
    - "D-05: Revenue mix is derived from stored results and assumptions with warnings when missing."
    - "D-06: Scenario comparison only uses available stored scenarios/results."
    - "D-07: Investor thesis/risk/roadmap content is deterministic template content."
    - "D-08: No OpenRouter/LLM work in Phase 4."
  artifacts:
    - path: "backend/app/services/investor_case.py"
      provides: "Investor aggregation service"
      contains: "build_investor_case"
    - path: "backend/app/routers/investor.py"
      provides: "Investor API"
      contains: "investor-case"
---

<objective>
Build backend investor dashboard API and investor-facing data aggregation.
</objective>

<context>
@.planning/phases/04-investor-case-data-quality/04-CONTEXT.md
@.planning/phases/04-investor-case-data-quality/04-RESEARCH.md
@.planning/phases/04-investor-case-data-quality/04-PATTERNS.md
@docs/blueprint.md
@.planning/phases/03-strategy-dashboard-ranking/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add investor schemas and aggregation service</name>
  <files>backend/app/schemas/investor.py, backend/app/services/investor_case.py, backend/tests/test_investor.py</files>
  <action>Create investor response schemas and `build_investor_case`. Aggregate selected plant/scenario, latest ScenarioResult, latest UnitScoringResult, latest SensitivityResult run, FinancialAssumption, data gaps, KPIs, CAPEX structure, revenue mix, scenario comparison, risks/mitigations, roadmap, and why-this-wins. Preserve null economics and warnings.</action>
  <verify>cd backend && pytest -q tests/test_investor.py -k "service or aggregate"</verify>
</task>

<task type="auto">
  <name>Task 2: Add investor API</name>
  <files>backend/app/routers/investor.py, backend/app/main.py, backend/tests/test_investor.py</files>
  <action>Add `GET /api/investor-case?plant_id=&scenario_id=`. If `scenario_id` is omitted, choose the latest scenario for the selected plant. Return 404/400 for invalid plant/scenario references.</action>
  <verify>cd backend && pytest -q tests/test_investor.py -k "api"</verify>
</task>

<task type="auto">
  <name>Task 3: Verify investor API slice</name>
  <files>.planning/phases/04-investor-case-data-quality/04-01-SUMMARY.md</files>
  <action>Run investor tests and write summary artifact.</action>
  <verify>cd backend && pytest -q tests/test_investor.py && rg -n "build_investor_case|investor-case|Investment thesis" backend</verify>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q tests/test_investor.py` exits 0.
- [ ] `rg -n "build_investor_case|investor-case|Investment thesis" backend` returns matches.
</verification>

<output>
Create `.planning/phases/04-investor-case-data-quality/04-01-SUMMARY.md`.
</output>
